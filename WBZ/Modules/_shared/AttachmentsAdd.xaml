<Window x:Class="WBZ.Modules._shared.AttachmentsAdd"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:se="clr-namespace:StswExpress;assembly=StswExpress"
        mc:Ignorable="d" SizeToContent="Height" ResizeMode="CanMinimize" WindowStartupLocation="CenterOwner"
        Title="{Tr AttachmentsNew}" Icon="/Resources/32/icon32_photos.ico"
        Loaded="Window_Loaded">
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="0,0,0,1" CornerRadius="0"/>
    </WindowChrome.WindowChrome>
    <DockPanel>
        <!-- titleBar -->
        <se:TitleBar DockPanel.Dock="Top" SubIcon="/Resources/32/icon32_add.ico"/>
        <!-- main -->
        <DockPanel DockPanel.Dock="Top">
            <se:IconButton Margin="5,5,0,5"
                           ToolTip="{Tr FromBrowser}" IsTextVisible="False"
                           IsEnabled="{Binding EditMode}"
                           Icon="/Resources/32/icon32_web.ico"
                           Click="btnLink_Click"/>
            <TextBox x:Name="tbLink" Margin="5" VerticalContentAlignment="Center"
                     Width="{Binding FontSize, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}, Mode=OneWay, Converter={se:conv_Size}, ConverterParameter=30}"/>
        </DockPanel>
        <DockPanel DockPanel.Dock="Top">
            <se:IconButton Margin="5,5,0,5"
                            ToolTip="{Tr FromDrive}" IsTextVisible="False"
                            IsEnabled="{Binding EditMode}"
                            Icon="/Resources/32/icon32_folder.ico"
                            Click="btnDrive_Click"/>
            <TextBox x:Name="tbDrive" Margin="5" VerticalContentAlignment="Center"
                     Width="{Binding FontSize, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}, Mode=OneWay, Converter={se:conv_Size}, ConverterParameter=30}"/>
        </DockPanel>
        <DockPanel x:Name="dpName" DockPanel.Dock="Top">
            <Label Content="{Tr Name, Suffix=':'}" Margin="5,5,0,5" VerticalAlignment="Center"/>
            <TextBox x:Name="tbName" Margin="5" VerticalContentAlignment="Center"/>
        </DockPanel>
        <se:IconButton Margin="5" HorizontalAlignment="Center"
                        Text="{Tr Accept}"
                        Icon="/Resources/32/icon32_accept.ico"
                        IsDefault="True"
                        Click="btnAccept_Click"/>
    </DockPanel>
</Window>
