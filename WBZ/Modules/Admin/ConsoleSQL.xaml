<Window x:Class="WBZ.Modules.Admin.ConsoleSQL"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:c="clr-namespace:WBZ.Controls"
        xmlns:h="clr-namespace:WBZ.Helpers"
        xmlns:p="clr-namespace:WBZ.Properties"
        mc:Ignorable="d"
        Title="Konsola SQL" MinHeight="180" MinWidth="270" Height="450" Width="675"
            BorderThickness="6" BorderBrush="#333" ResizeMode="CanResize" WindowStartupLocation="CenterScreen" WindowStyle="None"
            FontFamily="{Binding config_FontFamily, Source={x:Static p:Settings.Default}}" FontSize="{Binding config_FontSize, Source={x:Static p:Settings.Default}}"
            Icon="pack://siteoforigin:,,,/Resources/icon32_source.ico">
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="0,0,0,1" CornerRadius="0"/>
    </WindowChrome.WindowChrome>
    <DockPanel>
        <!-- titleBar -->
        <c:TitleBar DockPanel.Dock="Top"/>
        <!-- main -->
        <DockPanel DockPanel.Dock="Bottom">
            <c:SimpleButton Margin="5,5,0,5" IsEnabled="True" TextVisibility="False" HorizontalAlignment="Left"
                            IconSource="pack://siteoforigin:,,,/Resources/icon32_refresh.ico" PreviewMouseLeftButtonUp="btnStart_Click"/>
            <Label Margin="5,5,0,5" Content="Mode:" VerticalAlignment="Center"/>
            <Label Margin="0,5" Content="only selecting data allowed" VerticalAlignment="Center" FontWeight="Medium"/>
        </DockPanel>
        <TextBox x:Name="tbConsole" DockPanel.Dock="Top" Margin="5" TextWrapping="Wrap" AcceptsReturn="True" AcceptsTab="True"  Height="{Binding ActualHeight, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}, Converter={h:conv_Size}, ConverterParameter=0.4}"/>
        <DataGrid x:Name="dgConsole" DockPanel.Dock="Top" Margin="5" Padding="5" HorizontalGridLinesBrush="LightGray" VerticalGridLinesBrush="LightGray" BorderThickness="1"
                  AutoGenerateColumns="True" RowHeaderWidth="0" IsReadOnly="True"/>
    </DockPanel>
</Window>
